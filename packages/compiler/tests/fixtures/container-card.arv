theme {
  container-size {
    wide = 480px;
  }
}

component Card {
  base {
    container-type: inline-size;
    display: block;

    @container (inline-size < container-size.wide) {
      display: flex;
    }
  }
}
