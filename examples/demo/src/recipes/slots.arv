component RecipeSlotsButton {
  slots {
    icon { flex-shrink: 0; }
    label { font-weight: 500; }
  }

  base {
    display: inline-flex;
    align-items: center;
    gap: space.2;
    padding: space.2 space.4;
    border-radius: radius.md;
    background: color.primary;
    color: white;
    border: none;
    font: inherit;
  }
}
