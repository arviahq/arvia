// `pulse` is declared as a raw `@keyframes` in theme.arv; reference it by literal name.
component KeyframesDemo {
  base {
    padding: space.4 space.5;
    border-radius: radius.md;
    background: color.primary;
    color: white;
    font-size: font.sm;
    animation: pulse duration.fast;
  }
}
