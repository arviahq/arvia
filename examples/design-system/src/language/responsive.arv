component ResponsiveDemo {
  base {
    display: inline-flex;
    border: none;
    border-radius: radius.md;
    background: color.primary;
    color: white;
    font: inherit;
    cursor: pointer;

    // Small by default; bumps up at the md breakpoint via a raw @media min-width condition.
    padding: space.1 space.3;
    font-size: font.sm;

    @media (min-width: breakpoint.md) {
      padding: space.3 space.5;
      font-size: font.lg;
    }
  }
}
